name: CI

on:
  push:
    branches: [ "main", "CI/CD" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Set up Python for FastAPI
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install FastAPI dependencies
        run: |
          cd FastAPI
          if [ ! -f requirements.txt ]; then
            echo "requirements.txt not found. Creating a placeholder."
            echo "# Add your dependencies here" > requirements.txt
          fi
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Set up Node.js for React
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install React dependencies
        run: |
          cd React/ansible-platform
          npm install --force

      # Run backend tests (if available)
      - name: Run FastAPI with Uvicorn to check server start
        run: |
          cd FastAPI
          uvicorn main:app --host 127.0.0.1 --port 8000 --reload &
          sleep 5
          curl --fail http://127.0.0.1:8000/docs || (echo "Uvicorn server failed to start" && exit 1)


      # Build frontend and run tests
      - name: Build React app
        run: |
          cd React/ansible-platform
          npm run build

      - name: Run React tests
        run: |
          cd React/ansible-platform
          npm test || echo "No tests found"
